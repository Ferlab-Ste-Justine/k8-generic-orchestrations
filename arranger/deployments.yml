---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: arranger-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: arranger-server
  template:
    metadata:
      labels:
        app: arranger-server
    spec:
      containers:
        - image: overture/arranger-server:98162e0
          name: arranger-server
          resources:
            requests:
              memory: "128Mi"
              cpu: '0.25'
            limits:
              memory: "256Mi"
              cpu: '0.50'
          envFrom:
            - configMapRef:
                name: arranger-server